apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ .Release.Name }}-secrets-provider
  namespace: {{ .Release.Namespace | default "training-portal" }}
spec:
  provider: azure
  secretObjects:
    - secretName: {{ .Values.secretName }}
      type: Opaque
      data:
        - objectName: pg-connection-string
          key: PG_CONNECTION_STRING
        - objectName: user-portal-client-id
          key: AUDIENCE
        - objectName: openai-endpoint
          key: AZURE_OPENAI_ENDPOINT
        - objectName: openai-deployment-name
          key: AZURE_OPENAI_DEPLOYMENT
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    useWorkloadIdentity: "true"
    keyvaultName: "{{ .Values.keyvaultName }}"
    tenantId: "{{ .Values.tenantId }}"
    clientID: "{{ .Values.workloadIdentity.clientId }}"
    objects: |
      array:
        - |
          objectName: pg-connection-string
          objectType: secret
        - |
          objectName: user-portal-client-id
          objectType: secret
        - |
          objectName: openai-endpoint
          objectType: secret
        - |
          objectName: openai-deployment-name
          objectType: secret

